# Bản Vision V2 chạy độc lập (tham khảo)

Thư mục này chỉ giữ lại bản test OpenCV cũ để đối chiếu. Bản chính đã được ghép
vào `python main.py`. Không chạy file ở đây đồng thời với app vì camera chỉ nên
được một chương trình sử dụng.
