{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chạy ứng dụng chính",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}\\main.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "python": "${command:python.interpreterPath}"
    }
  ]
}
